body { font-family: 'Segoe UI', sans-serif; background: #f4f7f6; padding: 20px; color: #333; }
header { text-align: center; margin-bottom: 20px; }
.config-section, .ingredientes-section { background: white; padding: 15px; border-radius: 12px; box-shadow: 0 4px 6px rgba(0,0,0,0.1); margin-bottom: 20px; }
select { width: 100%; padding: 10px; border-radius: 8px; border: 1px solid #ccc; font-size: 1rem; }
.grid-ingredientes { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; }
label { display: flex; align-items: center; gap: 8px; font-size: 0.9rem; background: #eee; padding: 8px; border-radius: 6px; cursor: pointer; }
button { width: 100%; padding: 15px; background: #27ae60; color: white; border: none; border-radius: 8px; font-size: 1.1rem; font-weight: bold; cursor: pointer; }
.ticket { border-left: 5px solid #27ae60; background: #e8f5e9; padding: 15px; margin-top: 10px; }
.resultado-oculto { display: none; }
